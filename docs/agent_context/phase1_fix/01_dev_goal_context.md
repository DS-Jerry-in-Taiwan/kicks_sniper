# Phase 1 Fix - 開發目標：8891 爬蟲架構遷移

**階段**: Phase 1 Hotfix - 解決動態渲染問題
**母階段**: Phase 1 - 8891 爬蟲原型
**目的**: 解決原始 Phase 1 使用 requests 無法抓取 CSR 網頁的問題，遷移至 Playwright。

## 🎯 Hotfix 目標
1. **架構遷移**: 將爬蟲核心從 `requests` 替換為 `Playwright (Async)`.
2. **環境修復**: 安裝瀏覽器自動化所需的相依套件與二進位檔.
3. **功能恢復**: 確保能重新抓取到 8891 的車輛資料 (Price, Year, Mileage).
4. **除錯增強**: 增加截圖功能以應對 Headless 模式下的除錯需求。

## 📦 交付物清單
1. `src/crawlers/crawler_8891.py`: **(覆寫)** 基於 Playwright 的新版爬蟲.
2. `requirements.txt`: **(更新)** 加入 playwright.
3. `logs/debug_8891.png`: 執行截圖.
4. `data/raw/kicks_list.json`: 驗證用的數據。

## ✅ 驗收標準
- [ ] `crawler_8891.py` 能成功執行且無崩潰.
- [ ] 截圖 `debug_8891.png` 顯示車輛列表畫面 (非空白/403).
- [ ] `kicks_list.json` 包含有效數據 (筆數 > 0).
