# Phase 2.5 - Checkpoint 協議

## 🚦 Checkpoint 1: 分頁邏輯確認 (After @CODER)
**觸發時機**: Coder 完成腳本修改，準備執行第一次全量抓取時。
**檢查重點**:
1. **無限迴圈防護**: 是否有設定 `max_pages` (例如最多抓 20 頁) 作為安全保險？避免邏輯錯誤導致死循環。
2. **等待機制**: 是否有足夠的 `wait` 時間？太快會被 8891 封鎖 IP。

**決策**:
- ✅ **通過**: 執行腳本。
- 🔄 **修正**: 若沒加 `sleep` 或 `wait`，請務必加上。

---

## 🚦 Checkpoint 2: 數據完整性確認 (After @ANALYST)
**觸發時機**: 抓取完成後。
**檢查重點**:
1. **數量對比**: 去 8891 官網看 Kicks 總數 (例如 88 台)，檢查 JSON 是否也是 88 台 (容許誤差 ±5)。
2. **資料庫狀態**: 執行 `src/main.py` 後，DB 裡面的資料是否正常？

**決策**:
- ✅ **通過**: 支線任務完成，回歸 Phase 3 主線。
- ❌ **失敗**: 若只抓到第一頁，代表 Next Button Selector 錯誤，需修復。

